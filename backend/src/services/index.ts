/**
 * Backend services: quote, cart, order, payment, upload, audit.
 * Domain logic and external I/O live here; API routes call into these modules.
 */

// Services will be implemented in Phase 3+ (e.g. quote-service, cart-service, checkout-service).
export {};
